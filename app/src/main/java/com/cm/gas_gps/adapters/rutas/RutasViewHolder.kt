package com.cm.gas_gps.adapters.rutas

import android.content.Context
import androidx.recyclerview.widget.RecyclerView
import com.cm.gas_gps.databinding.ItemRutasBinding

class RutasViewHolder (
    private val binding: ItemRutasBinding,
//    private val onClickHistorial:(Int) -> Unit,
//    private val ctx: Context,
//    private val viewModel: HistorialPagosViewModel
): RecyclerView.ViewHolder(binding.root) {

//    fun bind(model: dtoHistorialPagosModel) {
//        with(binding) {
//
//            val date = Date.parse(model.fechaPago)
//            val formatUno = SimpleDateFormat("dd/MM/yyyy HH:mm:ss", Locale("es", "MEX"))
//            fecha.text = formatUno.format(date)
//
//            txtReferenciaAdapter.text = model.referencia
//            txtTotalAdapter.text = model.total
//            txtSucursalAdapter.text = model.NombreSucursal
//
//            val df = DecimalFormat("$#,###.00")
//            df.roundingMode = RoundingMode.DOWN
//            val roundoff = df.format(model.total.toBigDecimal())
//
//            txtTotalAdapter.text = roundoff
//
//            root.setOnClickListener {
//                viewModel.loadingPDF(model)
//            }
//        }
//    }


}